#include <stdio.h>
#include <string.h>

int main(int argc, char**argv)
{
    if(1)
    //if(1) is the other side here

    //0=false and 1=true
    //so the if statement will NOT execute (the else statement will execute
    {
       printf("Execute this statement!");
    }
    else
    {
        printf("In the else statement.");
    }
    //a function that returns as an int can also work as true or false
}
